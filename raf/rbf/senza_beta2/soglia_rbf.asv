function vet_soglia = soglia_rbf(ac,num,th)
% vet_soglia = soglia(ac,num) pone una soglia di decisione sul valore
% dell'uscita del neurone di output della rbf

for d = 1:num
    if ( ac(1,d) > th ) 
        vet_soglia(d) = 2;
        else vet_soglia(d) = 1;
    end

end    
